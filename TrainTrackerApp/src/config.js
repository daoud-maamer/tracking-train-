// Configuration for API and Network
// Change this IP address to your current machine's Local IPv4 address when moving between networks.
// Example: If ipconfig says your IP is 192.168.1.45, change it here.

export const LOCAL_IP = '192.168.1.8';
export const API_BASE = `http://${LOCAL_IP}:3000`;
export const API_BASE_URL = `${API_BASE}/api`;
